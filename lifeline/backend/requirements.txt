# Lifeline Python bridge — intentionally minimal.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
pymongo>=4.10.0
httpx>=0.27.0
python-dotenv>=1.0.0

# Jac 0.34+ is distributed as a standalone binary, not a Python package.
# Install it with the official installer; see the root Dockerfile / README.
